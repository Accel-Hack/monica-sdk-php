{
  "name": "ah-monica/monica",
  "description": "Framework-independent PHP error monitoring SDK for MONICA",
  "license": "Apache-2.0",
  "type": "library",
  "require": {
    "php": ">=7.4",
    "ext-json": "*",
    "ext-zlib": "*",
    "psr/http-client": "^1.0",
    "psr/http-factory": "^1.0",
    "psr/http-message": "^1.0 || ^2.0"
  },
  "suggest": {
    "ext-curl": "Required by the default HTTP transport when no PSR-18 client is supplied"
  },
  "require-dev": {
    "nyholm/psr7": "^1.8"
  },
  "autoload": {
    "psr-4": {
      "Monica\\": "src/"
    }
  },
  "bin": [
    "bin/monica"
  ],
  "scripts": {
    "spec:sync": "php scripts/spec-sync.php",
    "spec:check": "php scripts/spec-sync.php --check",
    "spec:check-remote": "php scripts/spec-sync.php --check-remote",
    "test": "php tests/run.php && php tests/fatal-runner.php && php tests/spec-contract.php"
  },
  "scripts-descriptions": {
    "spec:sync": "MONICA の公開契約バンドルを spec/ へ取り込み直す",
    "spec:check": "取り込んだ spec/ が spec.lock.json と一致するか（network 不要）",
    "spec:check-remote": "配信元の公開契約が動いていないか"
  },
  "support": {
    "issues": "https://github.com/Accel-Hack/monica/issues",
    "source": "https://github.com/Accel-Hack/monica"
  }
}
